<header>
  <nav>
    <div class="icons">
      <ul class="ulicons">
        <li><a href="#" class="fa fa-tiktok"></a></li>
        <li><a href="#" class="fa fa-facebook"></a></li>
        <li><a href="https://www.instagram.com/falang.noi/?hl=da" class="fa fa-instagram"></a></li>
      </ul>
    </div>

    <div>
      <div class="min-nav">
        <ul>
          <li><a href="#">Gallery</a></li>
          <li><a href="#">About</a></li>
        </ul>
      </div>
    </div>
  </nav>
</header>

<style>
  li {
    list-style-type: none;
  }

  nav {
    display: flex;
    flex-direction: row;
    margin: 1.5rem;
  }

  a {
    text-decoration: none;
    color: var(--primary);
  }

  .minnav ul {
    gap: 1rem;
    list-style: none;
    display: flex;
    flex-direction: column;
  }

  .icons ul {
    display: flex;
    flex-direction: row;
    gap: 1rem;
    list-style: none;
  }

  .ul {
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }

  nav {
    display: flex;
    justify-content: end;
    gap: 2rem;
    flex-direction: row;
  }
  .fa {
    font-size: 1.4rem;
  }
  .fa-instagram {
    color: var(--primary);
  }
  .fa-facebook {
    color: var(--primary);
  }

  .fa-tiktok {
    color: var(--primary);
  }

  @media (min-width: 800px) {
  }
</style>
