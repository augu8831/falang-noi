<header>
  <nav>
    <div class="icons">
      <ul class="ulicons">
        <li><a href="https://www.instagram.com/falang.noi/?hl=da" class="fa-brands fa-instagram"></a></li>
        <li><a href="#" class="fa-brands fa-facebook-f"></a></li>
        <li><i class="fa-brands fa-tiktok"></i></li>
      </ul>
    </div>
    <div>
      <div class="minnav">
        <ul>
          <li><a href="/index.html">HOME</a></li>
          <li><a href="/about.html">ABOUT</a></li>
          <li><a href="/gallery.html">GALLERY</a></li>
        </ul>
      </div>
    </div>
  </nav>
</header>

<style>
  li {
    list-style-type: none;
  }

  nav {
    display: flex;
    padding: 1.5rem;
    align-items: flex-end;
    justify-content: end;
    gap: 2rem;
    flex-direction: column-reverse;
  }

  a {
    text-decoration: none;
    color: var(--primary);
    font-family: "ivyepic", sans-serif;
    font-weight: 600;
    font-style: normal;
  }

  .minnav ul {
    gap: 0.5rem;
    list-style: none;
    display: flex;
    flex-direction: column;
  }

  .icons ul {
    display: flex;
    flex-direction: column;
    gap: 1rem;
    list-style: none;
  }

  .ul {
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }
  .fa-brands {
    font-size: 1.4rem;
  }
  .fa-instagram {
    color: var(--primary);
  }
  .fa-facebook-f {
    color: var(--primary);
  }

  .fa-tiktok {
    color: var(--primary);
  }

  @media (min-width: 800px) {
    li {
      list-style-type: none;
    }

    nav {
      display: flex;
      padding: 1.5rem;
      justify-content: end;
      align-items: flex-start;
      gap: 2rem;
      flex-direction: row;
    }

    a {
      text-decoration: none;
      color: var(--primary);
      font-family: "ivyepic", sans-serif;
      font-weight: 600;
      font-style: normal;
    }

    .minnav ul {
      gap: 0.5rem;
      list-style: none;
      display: flex;
      flex-direction: column;
    }

    .icons ul {
      display: flex;
      flex-direction: row;
      gap: 1rem;
      list-style: none;
    }

    .ul {
      display: flex;
      flex-direction: column;
      gap: 1rem;
    }
    .fa-brands {
      font-size: 1.5rem;
    }
    .fa-instagram {
      color: var(--primary);
    }
    .fa-facebook-f {
      color: var(--primary);
    }

    .fa-tiktok {
      color: var(--primary);
    }
  }
</style>
